# Web 框架
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# 数据库
SQLAlchemy>=2.0.36
# PostgreSQL 驱动（部署到 Render 时用；本地 SQLite 不需要）
psycopg2-binary>=2.9.9

# 数据校验
pydantic>=2.10.0
pydantic-settings>=2.6.0

# GitHub OAuth 请求
httpx>=0.28.0

# JWT 会话
PyJWT>=2.10.0

# 从 .env 读取环境变量（pydantic-settings 已内置，这里保留兼容）
python-dotenv>=1.0.1
